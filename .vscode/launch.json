{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug supermarket_inventory (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\supermarket_inventory.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "build supermarket_inventory (MSVC)"
        }
    ]
}
